# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'MCP Memory Dashboards'
    orgId: 1
    folder: 'MCP Memory'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json