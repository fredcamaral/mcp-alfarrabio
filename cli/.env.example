# Lerian MCP Memory CLI - Overrides
# 
# This file is for CLI-specific overrides only.
# The CLI reads configuration from the root .env file.
# Only uncomment values here if you need different settings than the root configuration.

# =================================================================
# CLI-SPECIFIC OVERRIDES (rarely needed)
# =================================================================

# Override the config directory location (default: ~/.lmmc)
# LMMC_CONFIG_DIR=/custom/path/to/lmmc/config

# Override server connection (if different from root .env)
# LMMC_SERVER_URL=http://different-server:9080
# LMMC_SERVER_TIMEOUT=60

# Override CLI display preferences
# LMMC_OUTPUT_FORMAT=json                 # Options: table, json, plain
# LMMC_COLOR_SCHEME=never                 # Options: auto, always, never
# LMMC_PAGE_SIZE=50                       # Number of items per page
# LMMC_EDITOR=vim                         # Editor for task editing

# Override logging settings
# LMMC_LOG_LEVEL=debug                    # Options: debug, info, warn, error
# LMMC_LOG_FORMAT=json                    # Options: text, json

# =================================================================
# NOTES
# =================================================================
#
# 1. The CLI automatically reads from the root project's .env file
# 2. All LMMC_* variables are defined in the root .env.example
# 3. This file is only for overrides when you need different settings
# 4. Most users don't need this file at all
# 5. API keys (OPENAI_API_KEY, etc.) are inherited from root .env